Dart 언어 naming 규칙

1. 파일명 & 폴더명
- 첫번째 글자는 소문자
- 여러 단어 조합할  경우, 이어지는 단어들 앞에 _ (underScore) 입력
- ex) button_screen -> snake case 라고 함

2. 클래스명 (class)
- 첫번째 글자 대문자
- 여러 단어 조합 시 이어지는 첫글자 대문자로 입력
- ex) ButtonScreen -> camel case

3. 변수명 & 함수명
- 첫번째 글자 소문자
- 여러 단어 조합 시 이어지는 첫글자 대문자로 입력
-> listItem()